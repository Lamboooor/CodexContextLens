# 上下文准确性与界面设计

验证日期：2026-09-25。版本：0.3.0。

## 哪些数字可以核对

最近输入、输出、缓存、推理及累计 token 直接读取 Codex 日志计数器；缓存是输入的子集，推理是输出的子集，不能再次相加。累计消耗会反复计入多次请求的历史输入，不等于当前上下文大小。

本机官方扩展 `openai.chatgpt-26.917.62051-win32-x64` 的前端上下文函数读取 `last.totalTokens` 和 `modelContextWindow`，计算 `min(totalTokens, window) / window * 100`，界面四舍五入成整数百分比。本插件按同一公式读取日志中的 `last_token_usage.total_tokens` 和 `model_context_window`。缺少总数字段时使用输入加输出；缺少窗口时不猜测模型容量。

`test/calibrate.js` 只提取官方包中的小型纯计算函数，在隔离 VM 中运行，不启动官方应用或调用模型。与最近三份日志的 94 个 token_count 快照逐个比较，公式和解析器结果均零差异；另有 48 个边界组合通过。旧的 input-only 公式在上述 94 个快照均有偏差，最大为 5.0859 个百分点。非法无限值额外拒绝，与原生函数在非 JSON 输入上的行为刻意不同。

校准函数 SHA256：`928a3237dc57d537dc5c8a0e0b2a08f9b8b5f3309ee94aa05dcfab1671088819`。官方包升级或实现改变后需重新审查；此结论不代表所有版本都采用这一公式。

## 尚不能证明什么

- 这是已记录请求的快照，不是模型服务器当前内存。新输入、正在生成的输出及待发送工具结果可能尚未计入。读取时间与用量记录时间分开展示。
- 比较的是相同日志数字在原生函数中的结果，未自动操作官方聊天窗口，未完成同一时刻的双界面截图比对。
- 发现压缩事件后暂时隐藏占用率，等待新的用量记录；压缩阈值不等于上下文上限。
- 会话选择为固定会话或工作区最近会话，不跟随官方聊天焦点。人工比对时应先选同一会话、等待响应结束，核对记录时间，再比较整数百分比。
- 内容构成按日志可见文本 UTF-16 长度分类，既不是 token 分账，也不是模型仍保留的完整上下文。隐藏提示、图像和未公开的服务端处理无法从日志恢复。使用 tokenizer 也不能补回不可见内容，因此不把字符百分比乘总 token 来生成虚假的精确分类。

官方 [App Server 文档](https://learn.chatgpt.com/docs/app-server) 公开了 `thread/tokenUsage/updated` 用量更新通知，但没有提供此处所需的完整按历史、文件、指令分类的 token 明细。另起 app-server 也不等于订阅正在运行的 VS Code 会话。本版本继续只读日志，避免修改会话。

## 设计参考

- [CodexBar](https://codexbar.app/)：主要指标突出显示，进度与次要信息分层。
- [Claude/Codex Usage Statusbar](https://marketplace.visualstudio.com/items?itemName=solux-dev.cc-statusbar)：状态栏入口、悬浮摘要和详细面板分工。

本插件采用圆环、大数字、三张指标卡、构成色条和前几项图例。全部八类和精确计数放在详细面板。SVG 完全由本地代码生成，无远程图片和脚本，日志标签转义，图片附带文本替代说明。0.5.2 使用正式 tooltip 自动同步，变化以固定 5 秒窗口合并，相同内容不重写；实时数据在侧边栏和完整面板中局部更新。原生卡片自动或手动更新仍可能触发整卡重绘。
