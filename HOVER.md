# 0.5.2：自动同步悬浮与实时视图

底部 Codex Lens 点击后打开资源管理器里的“Codex Lens 实时用量”侧边栏视图。命令“打开用量面板”仍提供完整编辑器面板。

侧边栏与完整面板监听日志变化，保留已有 DOM 节点，只更新文本、样式和必要的列表变化；不重新设置 webview.html。焦点、滚动和已展开详情保留。

悬浮卡片默认自动同步日志中的最新数据，不必点击刷新。首次数据立即显示；后续变化使用固定 5 秒合并窗口，连续事件不会延后窗口。相同内容不重新发布，手动刷新、切换会话或主题可以立即更新。关闭设置 `codexLens.autoRefreshHover` 后才使用手动快照模式。

原生状态栏 tooltip 不支持局部修改 DOM，也没有正式鼠标移入事件。这是后台自动同步，不是每次悬停触发读取；卡片显示期间遇到更新仍可能整卡重绘。需要持续观察且避免整卡重绘时使用实时侧边栏。0.5.0/0.5.1 为解决闪烁把悬浮固定成手动快照，0.5.2 恢复自动更新并明确这一限制。

启动即读取已有日志，无须新对话。先查找当前工作区的主会话，列表数量限制不再让内部会话挤掉它；匹配不到时展示最近主会话并明确标注回退来源。固定会话仍优先。

使用稳定 API，无需实验开关。升级后运行 Developer: Reload Window。
